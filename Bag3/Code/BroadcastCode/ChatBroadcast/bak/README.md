# Outdated

Everything under this directory is stale, kept by the user's direction rather
than deleted (see `PHASE6_HANDOFF.md` §5.8). It predates the current
ChatBroadcast tree (`index_pyscript_backup.html`, `pyscript.json` and
`RS232.py` are from a PyScript-based prototype that has since been replaced;
`oldknowledge.py` predates the current `knowledge/` split; `js/` and `main.py`
are earlier drafts). None of it is imported or referenced by anything live in
`ChatBroadcast/`.
