One-line: the pink→purple gradient gem with a ✦ plus the product name — the only brand lockup that exists.

```jsx
<BrandMark />
<BrandMark size="splash" />
```

No logo file ships with the source. Never substitute a drawn mark; if the gem cannot be used, set the name in Nunito 800.
