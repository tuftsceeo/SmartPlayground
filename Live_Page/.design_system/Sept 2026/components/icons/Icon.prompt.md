One-line: the only icon primitive — inline stroke SVGs at 1.8 weight, copied from the app's own icon file; never hand-draw a new glyph.

```jsx
<Icon name="wand" size={18} />
<Icon name="cable" size={40} color="var(--teal)" />
```

Names: wifi, box, plug, floppy, wand, sparkles, pencil-sparkles, mop-sparkles, nfcCard, close, trash, pencil, radio, radioOn, check, warning, modeServe, shakePhone, spinner, code, wrench, message-circle, library, folder-open, shuffle, send, download, cable, unplug, gamepad, square-arrow-out-up-right, smartphone-nfc, vibrate, circle, chevronDown, volume-2, grid-3x3, music, palette, tag, snowflake, rainbow, arrow-up, chef-hat, circle-check, save, home, brain-circuit.

The **sparkles family** carries the magic in this product: `wand` (Lucide *wand-sparkles*) for the wand itself and anything wand-related, `pencil-sparkles` for creating/authoring a game, `sparkles` for new or exciting, `mop-sparkles` for clearing or resetting.

Sizes in product: 12 (inline status), 15 (default, footer/icon buttons), 16–18 (nav, send), 40–46 (hero / empty state, strokeWidth 1.3).
